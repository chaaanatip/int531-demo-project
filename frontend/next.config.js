/** @type {import('next').NextConfig} */
const nextConfig = {
    // Produce static export when you run `next build`
    output: 'export',

    // optional: if you want plain paths like /about -> /about/index.html
    // trailingSlash: true,

    // If you use images or other runtime-only features, check compatibility.
}

module.exports = nextConfig
